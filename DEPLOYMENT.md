# 🚀 Инструкция по развертыванию на VPS

## Предварительные требования

- VPS с Ubuntu 20.04+ или Debian 11+
- Docker и Docker Compose установлены
- Доступ по SSH к серверу
- Telegram Bot Token (уже настроен в проекте)

## Шаг 1: Установка Docker (если не установлен)

```bash
# Обновление пакетов
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo apt install docker-compose-plugin -y

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER

# Выход и повторный вход для применения изменений
exit
```

## Шаг 2: Клонирование проекта

```bash
# Клонирование репозитория (замените на ваш URL)
git clone <your-repository-url> bandboats-support-bot
cd bandboats-support-bot
```

## Шаг 3: Настройка переменных окружения

Файл `.env` уже настроен, но вы можете изменить пароли для production:

```bash
# Отредактируйте .env
nano .env
```

Рекомендуемые изменения для production:
- Измените `POSTGRES_PASSWORD` на более сложный пароль
- Обновите `DATABASE_URL` с новым паролем
- Убедитесь, что `TELEGRAM_BOT_TOKEN` корректный

Пример `.env`:
```env
# Telegram
TELEGRAM_BOT_TOKEN="ваш_токен_бота"

# Database
POSTGRES_USER=bandboats
POSTGRES_PASSWORD=ваш_безопасный_пароль
POSTGRES_DB=bandboats_support
DATABASE_URL="postgresql://bandboats:ваш_безопасный_пароль@postgres:5432/bandboats_support?schema=public"

# Application
NODE_ENV=production
LOG_LEVEL=info
PORT=3000
```

## Шаг 4: Запуск приложения

```bash
# Сборка и запуск контейнеров
docker-compose up --build -d

# Проверка статуса
docker-compose ps

# Проверка логов
docker-compose logs -f bot
```

## Шаг 5: Проверка работы

После запуска вы должны увидеть в логах:
```
✅ Telegram bot started successfully
✅ Application ready!
🤖 Bandboats Support Bot is running in production mode
📱 Bot is listening for messages...
```

Протестируйте бота в Telegram:
- Найдите бота: @bandboats_bot
- Отправьте команду `/start`
- Бот должен ответить приветственным сообщением

## Управление приложением

### Остановка
```bash
docker-compose down
```

### Перезапуск
```bash
docker-compose restart
```

### Просмотр логов
```bash
# Все логи
docker-compose logs

# Только бот
docker-compose logs bot

# С отслеживанием
docker-compose logs -f bot
```

### Обновление после изменений в коде
```bash
# Остановка контейнеров
docker-compose down

# Получение изменений из репозитория
git pull

# Пересборка и запуск
docker-compose up --build -d
```

## Резервное копирование базы данных

### Создание бэкапа
```bash
docker exec bandboats-postgres pg_dump -U bandboats bandboats_support > backup_$(date +%Y%m%d_%H%M%S).sql
```

### Восстановление из бэкапа
```bash
cat backup_20240101_120000.sql | docker exec -i bandboats-postgres psql -U bandboats -d bandboats_support
```

## Мониторинг

### Проверка использования ресурсов
```bash
docker stats
```

### Проверка здоровья контейнеров
```bash
docker-compose ps
```

## Решение проблем

### Бот не отвечает
1. Проверьте логи: `docker-compose logs bot`
2. Убедитесь, что контейнеры работают: `docker-compose ps`
3. Проверьте подключение к БД: `docker-compose logs postgres`

### Ошибки при миграции
```bash
# Войдите в контейнер
docker exec -it bandboats-bot sh

# Запустите миграции вручную
npx prisma migrate deploy
```

### Очистка и полный перезапуск
```bash
# Остановка и удаление всех контейнеров
docker-compose down -v

# Пересборка с нуля
docker-compose up --build -d
```

## Безопасность

1. **Изменить пароли** в `.env` перед деплоем
2. **Закрыть порт PostgreSQL** (5432) для внешнего доступа в `docker-compose.yml`
3. **Настроить firewall** на VPS
4. **Регулярно обновлять** зависимости и Docker образы

## Производительность

Бот оптимизирован для работы в production:
- Multi-stage Docker build для минимального размера образа
- Только production зависимости в финальном образе
- Health checks для автоматического перезапуска
- Graceful shutdown при остановке

## Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что все переменные окружения настроены
3. Проверьте доступность Telegram API из контейнера
