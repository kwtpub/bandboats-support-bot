# Bandboats Support Bot

Telegram бот для системы техподдержки с админ-панелью и управлением тикетами.

## Возможности

### Для пользователей
- 📝 Создание тикетов с заголовком и описанием
- 💬 Ответы на сообщения администратора
- 👀 Просмотр своих тикетов
- ✅ Закрытие тикетов
- ✏️ Редактирование заголовка и описания
- 🔔 Уведомления о новых ответах

### Для администраторов
- 🔐 Отдельная админ-панель
- 📂 Просмотр всех открытых тикетов
- 📋 Просмотр назначенных тикетов
- 👤 Назначение тикетов себе
- 💬 Ответы на тикеты
- ✅ Закрытие тикетов
- 📊 Пагинация списков (5 тикетов на странице)
- 🔔 Уведомления о новых сообщениях от пользователей

## Технологии

- **TypeScript** - типизированный JavaScript
- **Node.js** - серверная платформа
- **Telegraf** - фреймворк для Telegram ботов
- **Prisma ORM** - работа с базой данных
- **PostgreSQL** - реляционная база данных
- **Docker** - контейнеризация
- **Clean Architecture** - архитектурный подход
- **DDD** - Domain-Driven Design

## Быстрый старт (Локальная разработка)

### Требования
- Node.js 20+
- PostgreSQL 14+
- npm или yarn

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/your-repo/bandboats-support-bot.git
cd bandboats-support-bot
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте `.env` файл:
```bash
cp .env.example .env
```

4. Настройте `.env`:
```env
TELEGRAM_BOT_TOKEN=your_bot_token
DATABASE_URL=postgresql://user:password@localhost:5432/bandboats_support
```

5. Примените миграции:
```bash
npx prisma migrate dev
```

6. Запустите в режиме разработки:
```bash
npm run dev
```

## Развертывание на VPS

### Быстрое развертывание с Docker

1. Установите Docker и Docker Compose на VPS
2. Клонируйте репозиторий
3. Настройте `.env` файл
4. Запустите скрипт развертывания:

```bash
./deploy.sh
```

### Подробные инструкции

См. [README.deployment.md](./README.deployment.md) для детальных инструкций по развертыванию на VPS.

## Команды

### Разработка
```bash
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка TypeScript
npm start            # Запуск собранного приложения
```

### База данных
```bash
npx prisma migrate dev    # Применить миграции (dev)
npx prisma migrate deploy # Применить миграции (prod)
npx prisma studio         # Открыть Prisma Studio
npx prisma generate       # Сгенерировать Prisma Client
```

### Docker
```bash
./deploy.sh                # Развернуть бота
docker-compose up -d       # Запустить контейнеры
docker-compose logs -f bot # Посмотреть логи
docker-compose restart bot # Перезапустить бота
docker-compose down        # Остановить контейнеры
./backup.sh                # Создать бэкап БД
```

## Структура проекта

```
bandboats-support-bot/
├── src/
│   ├── domain/              # Доменный слой (бизнес-логика)
│   │   ├── entities/        # Доменные сущности
│   │   ├── repositories/    # Интерфейсы репозиториев
│   │   ├── services/        # Доменные сервисы
│   │   └── errors/          # Доменные ошибки
│   ├── infrastructure/      # Инфраструктурный слой
│   │   ├── database/        # Конфигурация БД
│   │   ├── repositories/    # Реализации репозиториев
│   │   └── container/       # DI контейнер
│   ├── presentation/        # Слой представления
│   │   └── telegram/        # Telegram интерфейс
│   │       ├── commands/    # Обработчики команд
│   │       ├── handlers/    # Callback обработчики
│   │       └── middleware/  # Middleware
│   └── index.ts             # Точка входа
├── prisma/                  # Prisma схема и миграции
├── docker-compose.yml       # Docker Compose конфигурация
├── Dockerfile              # Docker образ
└── README.md               # Документация
```

## Архитектура

Проект построен на основе **Clean Architecture** и **Domain-Driven Design**:

- **Domain Layer**: Rich Domain Model с бизнес-логикой
- **Infrastructure Layer**: Работа с БД, внешними сервисами
- **Presentation Layer**: Telegram интерфейс, команды, обработчики

## Команды бота

### Для всех пользователей
- `/start` - Начало работы с ботом
- `/help` - Справка по командам
- `/newticket` - Создать новый тикет
- `/mytickets` - Мои тикеты
- `/ticket <ID>` - Просмотр тикета

### Только для администраторов
- `/admin` - Админ-панель
- `/alltickets` - Все тикеты
- `/assign <ticket_id> <user_id>` - Назначить тикет

## Резервное копирование

### Автоматическое (рекомендуется)

Добавьте в crontab:
```bash
# Бэкап каждый день в 3:00
0 3 * * * cd /path/to/bot && ./backup.sh

# Очистка старых бэкапов (>30 дней)
0 4 * * * find /path/to/bot/backups -name "*.sql.gz" -mtime +30 -delete
```

### Ручное

```bash
./backup.sh
```

## Лицензия

MIT

## Контакты

При возникновении проблем создайте issue в репозитории.
